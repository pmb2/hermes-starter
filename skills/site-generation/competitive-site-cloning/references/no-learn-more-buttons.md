# Learn more: the operator explicitly removed all "Learn More" links from service cards. 
# Service cards should be info-only (icon + title + description). No CTAs on them.
# This applies to ALL site generators, not just the clone template.